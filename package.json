{
  "name": "acf-acf-migrator",
  "private": true,
  "scripts": {
    "test": "cd dev && composer test",
    "test-active": "cd dev && composer test-active",
    "test-pending": "cd dev && composer test-pending",
    "test-all": "cd dev && composer test-all",


		"devmode": "claude --mcp-config=./_doc/mcp/mcp-dev.json",
		"devmode-c": "claude --mcp-config=./_doc/mcp/mcp-dev.json -c"
  }
}
